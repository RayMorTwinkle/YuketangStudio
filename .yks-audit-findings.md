# YuketangStudio 审查候选发现清单（待核对）

项目：/Users/Ray/Desktop/AI工作台/YuketangStudio —— Tampermonkey 用户脚本（雨课堂助手）。
关键背景：脚本带 @grant，运行在沙箱 isolated world；`window` ≠ 页面 `unsafeWindow`（代码里 `gm.uw`）。
`<script>` 注入的库（marked/mermaid/DOMPurify/html2canvas）落在 `unsafeWindow`；`@require` 的库（jspdf/MathJax）落在沙箱 `window`。DOM expando（`el.__vue__`）跨 world 不可见。
用户已报 bug：① PPT 对话无法解析 markdown 和 HTML 片段；②「跟随当前页」不跟随老师翻页。

每条给出 [文件:行号] 与断言。请逐条：核对源码是否属实（属实/不属实/部分属实）、打修复必要性分 0-10（10=必须修）、给一句理由与最小修法建议。

## A. 渲染/markdown 链

1. ai.js:482,524 `mdToHtml` 判断 `window.marked?.parse`，但 ensureMarked（env.js:78-84）把 marked 装到 `gm.uw` 上 → 沙箱 `window.marked` 恒 undefined → 永远走 fallback 解析器（不支持列表/表格/链接，escapeHtml 转义全部 HTML）→ 用户报的 markdown/HTML 不解析。
2. ai.js:437 `const blocks = []` 模块级共享、从不清理 → 流式 paint 每帧重解析累积全文 → 代码块重复 push，内存泄漏。
3. styles.css:406-513 整套 `.ai-answer`/`#ykt-ai-answer` 选择器是死的（气泡实际是 `.ykt-chat-msg.ai`/`.ykt-ai-msg.ai`，无 `ai-answer` 类、无 `ykt-ai-answer` id）→ 即使 marked 修好，表格/列表/代码块/公式对齐也无样式。
4. ai.js marked 路径：markdown 内嵌原始 HTML 透传进 `innerHTML`，无任何清洗（DOMPurify 只用于 .ykt-embed 块）→ AI 输出里的 `<img onerror>`/恶意 HTML 会在页面执行（修第 1 条后此洞才激活）。
5. chat.js 无 `root.__yksOnShow` 赋值（ai.js:96/presentation.js:247/settings.js:392 都有）→ 切 tab 到 PPT 对话时 `refreshCtxThumb` 不触发，上下文缩略图常为空。
6. chat.js:359-366 `agnesChat` 不传 `override`（ai.js:376-384 传 `getOverride()`）→ 用户配置了 Profile 也必须解锁开发者模式，否则报「开发者模式未解锁」→ PPT 对话对普通用户整体不可用。
7. ai.js:327 `buildUserText(pickCurrentSlide().slide, ...)` 传入 slide 而非 `slide.problem` → 「题干文本注入（比截图可靠）」永远只输出空标题，题干/选项丢失。
8. ai.js:142 `!(ui?.config?.aiSlidePickPriority === 'presentation')` 与 settings.js:343 存的布尔不匹配 → 「主界面优先」复选框无效，恒为主界面优先。
9. ai.js:88-90 `ykt:open-ai` 与 ui-api.js:132 重复监听；ai.js:111 欢迎气泡缺 `children.length` 检查可重复追加。
10. ai.js:307 `if (!text && !auto) { /* 空 */ }` 死代码。

## B. 跟随当前页 / Vue store 链

11. vuex-helper.js:8 `document.querySelector('#app')?.__vue__` 在沙箱读页面 expando → 恒 null → waitForVueReady 永不 resolve（每 100ms 一次 log.warn 刷屏 ×2 面板）、watchMainPageChange 永不注册 → 「跟随当前页」与 AI 面板「主界面当前页」识别双双失效。修法：`gm.uw.document.querySelector`。
12. presentation.js:229-238 watchMainPageChange 回调丢弃 slideId 参数，从不写 `repo.currentSlideId`（全仓写入点仅 presentation.js:427 与 actions.js:304）→ 即使修好 11，跟随仍指向旧页。修法：回调里 `repo.currentSlideId = String(slideId)` 并同步所属 currentPresentationId。
13. presentation.html:3-6 定义 `.slide-thumb.selected`，JS 实际用 `.active`；styles.css 只有 `.slide-thumb`/`:hover` 无 `.active` 规则 → 跟随高亮即使逻辑修好也不可见。
14. styles.css:157 `.ykt-presentation-panel .slide-index` 少了 `#`（应为 `#ykt-presentation-panel`）→ 页码角标无样式。
15. vuex-helper.js:66-77 移动版分支只监听 cards 数量变化，数量不变的重发/回跳不触发。
16. vuex-helper.js:98-112 waitForVueReady 无超时 → Vue 不存在时无限轮询 + warn 刷屏。

## C. 面板/Shell/工具链交互

17. 五个面板 close 按钮（chat.js:31, ai.js:56, presentation.js:199, settings.js:480, tutorial.js:17）只摘自身 `visible`，shell 的 `active-tab` 仍在 → 主面板里点 ✕ 得到「壳还在内容空了」而非关闭。
18. toolbar.js:100-105 主面板按钮自记 `active` 状态，shell ✕ 关闭不同步 → 关闭后再开需点两次。
19. index.js:66 周期刷新守卫 `document.querySelector('.ykt-panel.visible')` —— shell 子面板永久保留 visible 类 → 开过一次主面板后周期刷新永久失效。
20. index.js document-start 时 `document.body` 可能不存在，ui._mountAll()/installToolbar() 直接 appendChild → 潜在整脚本崩溃（无 DOM ready 守卫）。
21. shell.js:46/64-80 关闭 shell 或切 tab 不中止 chat/ai 在途流式请求 → 后台继续烧 token。
22. presentation.js:498-500 大图 `img.crossOrigin='anonymous'`——项目自己注释说 OSS 无 CORS 头 → 带 crossorigin 的 img 直接加载失败，右侧详情裂图。
23. presentation.js:376-378、587 服务端标题（课件名/课堂名）未转义进 innerHTML → HTML 注入面。
24. active-problems.js:37 `endTime=NaN` 时 `Math.max(0,NaN)=NaN`，`NaN<=0` false → 卡片显示「剩余 NaNs」且永不消失。
25. ui-api.js:14 先读 `_config.ai.kimiApiKey`，19 行才兜底 `_config.ai={}` → 存储数据 ai=null 时 TypeError 崩掉 UI 初始化。
26. ui-api.js:52-53 enableNotifyDrag 只钳下界不钳上界 → 弹窗可拖出屏幕够不回。

## D. 网络拦截/状态层

27. fetch-interceptor.js:8-9 patch 沙箱 `window.fetch` 而非 `gm.uw.fetch` → 页面 fetch 一条拦不到（死代码），只拦到脚本自己的请求。对照 ws/xhr 都正确用了 gm.uw。
28. actions.js/repo.js/fetch-interceptor.js 对 repo.slides 键类型不统一（upsertSlide 存原始类型、fetch 拦截存 String、onUnlockProblem 用原始类型 get）→ slideId 数字/字符串不一致时静默 miss，onUnlockProblem 直接 return → 丢题不提醒不作答。
29. actions.js:309-315 `repo.currentLessonId` 仅启动时提取一次；正则只认 `/lesson/fullscreen/v3/`（不含 `/lesson/student/v3/`）；SPA 切课不重提 → 课件存错桶、submitAnswer 带错 lessonId。
30. actions.js:243-245 + answer.js:142-148 双重延时：autoAnswerTime 已等过 autoAnswerDelay+rand，submitAnswer 内 autoGate 又 calcAutoWaitMs 等一遍 → 实际延时≈2 倍，易越过 endTime。
31. ws-interceptor.js:111-153 connectOrAttachLessonWS：`new WebSocket` 是原生构造器，不走 MyWebSocket handler，自建 socket 无 message 分发 → 自动进课堂收不到 unlockproblem/timeline；且 host 只分 pro/www，changjiang 落到 wss://www.yuketang.cn 大概率握手失败。
32. xhr-interceptor.js:20 `cb(JSON.parse(this.responseText))` 对 responseType=json/blob 的 XHR 抛错被 catch{} 吞掉；open() 丢弃 user/password 且强制 async=true。
33. actions.js:200 `for (const slide of pres.slides)` 假定 slides 必为数组，异常被 xhr.intercept 的 catch{} 无声吞掉。
34. presentation.js:340-351 课件过滤逻辑：三分支等价于「URL lessonId===repo.currentLessonId」，过滤空时 351 行回退全量 → 过滤器形同虚设。
35. actions.js:428-432 `_autoOnLessonClickStarted=true` 在路径检查 `return` 之前置位 → 首次在课堂页调用后，SPA 回首页再也不装 DOM 接管。
36. actions.js:478-479 tryApiJumpFirst 成功路径不重置 `_autoOnLessonClickInProgress`；541-542 MutationObserver 永不 disconnect（disconnect 的 setTimeout 被注释掉）。
37. ws-interceptor.js:38,40 与 xhr-interceptor.js:20 的 try/catch 吞掉回调内所有异常（含 actions 内部 bug）→ 至少 catch 里 log.err 留痕。
38. actions.js:223-237 onUnlockProblem：`limit` 缺失→endTime=NaN 或 limit=0→立即过期跳答；problem/slide 未命中直接 return 无重试（WS 先于课件 XHR 到达时丢题）；重复 unlock 覆盖 status 复位 answering。
39. actions.js:227,234 用服务端 dt+limit 对本地 Date.now() 判过期 → 时钟偏移误判。
40. ws-interceptor.js:50-54 `isRainClassroomWS` 匹配过宽（includes('/ws')），findUrl 递归可能把无关消息 url 写进 repo.currentSelectedUrl。
41. repo.js:17-27 setPresentation 只裁剪 localStorage 不裁剪内存 Map；`{id, ...data}` 若 data.id 存在会覆盖参数 id。
42. index.js:36-44 sessionStorage 防重载旗标整个 tab 会话只放行一次，失败后无第二次补救。
43. xhr-interceptor.js:263 checkinClass 任何失败都抛固定 'checkinClass HTTP 400' → 误导排障。

## E. AI/答题链

44. openai.js:168-215,121-163 chatCompletion/queryAI 设了 timeout 但没 ontimeout → GM_xhr 超时后 Promise 永不 settle → status.answering 永真，该题卡死无感知。
45. agnes.js:74-88,108-114 abort 用自定义 reason（name='Error'），降级判断只认 name==='AbortError' → 用户取消/超时后反而再发一个不可取消的 GM_xhr 请求（gmXhrStream 未接 signal）。
46. agnes.js:164-170 gmXhrStream 每次 onprogress new 一个 sseParser → 跨 progress 边界的 SSE 行被丢弃 → 降级路径流式随机丢字。
47. ai-format.js:182-196 parseAIAnswer 无「答案:」行时兜底取 lines[0] → prompt 要求的 `STATE: NO_PROMPT` 哨兵被解析成字母提交（'S'/'A','E','M','N','O','P','R','S','T'）→ 识别失败时提交垃圾答案，比不答更糟。
48. answer.js:34-57 xhrPost 无 timeout/ontimeout → 服务端挂起时 Promise 永不 settle → answering 卡死。
49. screenshoot.js:75-117 downloadImageAsBase64 用 Image+crossOrigin+canvas.toDataURL，而项目已有 CORS 免疫的 fetchAsDataURL → OSS 无 CORS 头时取图失败退到 DOM 截图 → 自动作答取图可靠性差。
50. screenshoot.js:9-14 captureProblemScreenshot 兜底 document.body 整页截图发给 AI —— 与 slide-image.js:53「绝不悄悄截整页」原则冲突。
51. actions.js:295 parseManual `content.split('').sort()` 不过滤分隔符 → 输入 'A、B' 提交 ['、','A','B']。
52. openai.js:298,425-437 queryAIVision 支持 problemType 合并，但调用方 actions.js:133 从不传 → 「后端题型优先」死代码，多选可能被 VLM 误判成单选。
53. answer.js:107 `okList.includes(problem.problemId)` 严格相等 → 服务端回 string 本地 number 时报「服务器未返回成功信息」（实际成功）。
54. answer.js:79 `${resp.msg}` 缺失时用户看到 "undefined (5)"。
55. screenshoot.js:89 jpeg 数据被 openai.js:231/373 包成 `data:image/png;base64,` → MIME 标注错误（多数端点容忍）。
56. ai-format.js:270-282 填空题 >50 字符返回 {content,pics} 对象而非数组，与 makeDefaultAnswer 的数组编码不一致；≤50 字符按逗号/空格切分，含空格合法答案被拆碎。
57. openai.js:51 DeepSeek 预设 visionModel='deepseek-chat' 非视觉模型 → 单步路径发图给文本模型必报错。
58. agnes.js:63 reasoning_effort 无条件下发 → 不支持的 OpenAI 兼容端点可能 400。
59. agnes.js:74-88 fetch 拿到 HTTP 4xx/5xx 明确错误也降级 GM_xhr 重发一次 → 重复计费。
60. userscript.meta.js @connect 列表硬编码已知 provider → 用户自定义 profile 的 baseUrl 不在表内时，GM_xhr 兜底必失败（自定义端点=完全不可用）。
61. actions.js:22-37 无 API Key 时自动提交默认答案（['A']/[' 1']/略）——「宁可错答」策略应做成显式开关。
62. openai.js:43 getActiveProfile 就地 mutate `p.baseUrl`（读函数写副作用，会被 autosave 落盘）。

## F. PDF/历史课件/构建

63. index.js:115 无标记地在任何 student-v3 页跑 runHistoryCapture → 用户手动浏览报告页也被强制开 lightbox + 自动下载 PDF。
64. 收集 tab 跑完整 main()：startAutoClickOnOnLessonBar 不排除 student-v3 → tryApiJumpFirst 可能把收集页导航走，收集死亡；且每个收集 tab 注入工具栏/拦截器属浪费。
65. history-capture.js:77-80 去重 key 含 cover 数字 m[2]，碰撞时 m[2]===prev.n → 「保留最高清晰度」else 分支永假（死代码），若 m[2] 是分辨率变体则同页多分辨率不去重。
66. env.js:59-64 ensureJsPDF fallback loadScriptOnce 注入主世界但检查 `window.jspdf` → fallback 是死代码；:68 注释「与 ensureJsPDF 同理」恰好说反。
67. pdf-export.js:96 全部图片失败时 doc===null，`doc.save` 抛裸 TypeError → 错误提示晦涩。
68. presentation.js:118 collectStaticSlideURLsFromDom 硬编码 `thu-private-qn.yuketang.cn` → 长江/其他部署的报告页课件收集恒为空（meta @connect 已声明 changjiang-private-qn）。
69. presentation.js:713-809 downloadPresentationPDF 重复实现 exportImagesToPdf 且更差：串行下载、单图失败整份中止、无去重、无 URL 页插空白页、onerror reject Event 导致 toast 显示 [object Event]。应直接调公共 exportImagesToPdf。
70. history-capture.js:141 GM_openInTab {active:true} 批量导入每个课堂抢焦点；:176 固定 180s 超时，大课件/慢网不够，超时还会 close 掉将完成的 tab。
71. storage.js/devmode.js:70 解密后的 apiKey 与用户自填 key 明文存页面同源 localStorage → 页面 JS/任意 XSS 可读 → 敏感配置应改 GM_setValue。
72. package.json:11-13 `npm test`/`check` 在干净检出必失败（test-devmode.mjs 被 gitignore 不存在）。
73. userscript.meta.js GM_getTabs 无调用；@match 大量冗余；`/m/*` 范围过宽。
74. history-capture.js:92-93 每张图一次 GM_setValue 进度推送 → 百页课件 100+ 次跨 tab 写，应节流。
75. history-capture.js:74 `src.includes('token')` 硬假设 slide URL 带 token，不带则收集为空且排查难。
76. history-capture.js:20 statusEl 用 innerHTML 拼接页面可控文本 → 改 textContent。
77. scripts/test-pdf-dedup.mjs:120 `typeof last.pages !== 'object'` 无效断言；全 data: URL 绕过 fetchAsDataURL 路径；MAE 阈值两侧未真实检验。
78. scripts/test-activities-paging.mjs stub 与源码 offset 语义循环论证，可能掩盖真实分页错位。
79. rollup.config.mjs:20-21 注释「devmode-blob.js 不在 git 里」与实际不符（文件在仓库中）。
80. index.js:1 文件带 BOM。

## G. 死代码/清理（低优先）

81. src/ai/kimi.js(216行)、deepseek.js(56行) 无任何 import；gemini.js/openrouter.js 空文件；openai.js queryAI 无调用方；actions.js startAutoAnswerLoop 双定义+tickAutoAnswer 无调用；env.js ensureFontAwesome 无调用（index.js 内联了一份）；toolbar.js isNarrowDevice 导出无调用；ai.js:119 `ykt-btn-ai`/presentation.js:280 `ykt-btn-pres`/tutorial.js:32 `ykt-btn-help` 查已删除按钮；types.js:49-59 DEFAULT_CONFIG 顶层 profiles/activeProfileId 是死配置（实际用 ai.profiles）；openai.js:1 头注释写 kimi.js；screenshoot.js:1 注释写 screenshot.js；presentation.html `.slide-thumb.selected` 死样式；styles.css 里 .ykt-problem-list/.text-status/.ykt-custom-prompt/.ai-question/.ai-loading/.ai-error/.ai-actions/.ykt-panel.dark 系列死规则。
82. ui-api.js:97-103 _bringToFront 对 notify 弹窗恒 no-op（无 visible 类）。
83. actions.js:477 `history.replaceState(null,'',location.href)` 自替换无操作。
84. active-problems.js:21 每秒全量重建 DOM，隐藏时也跑。
85. escapeHtml 在 ai.js/chat.js/auto-answer-popup.js 三处重复。
